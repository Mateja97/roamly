// Shared sizing for every Tripadvisor API-hosted rating-bubble image
// (aggregate lockup, subratings grid, review cards) — DESIGN_STANDARDS.md's
// "≥55px wide... contain" rule, one constant instead of three copies.
export const RATING_BUBBLE_WIDTH = 55;
export const RATING_BUBBLE_ASPECT_RATIO = 5;
