# pipeline/

Per-run artifacts (`research.md`, `product-tasks.md`, `design-spec.md`,
`engineering-notes.md`, `review-log.md`) written by the agent pipeline. These
are a local paper-trail, gitignored — see [docs/agent-pipeline.md](../docs/agent-pipeline.md)
for the process itself.
