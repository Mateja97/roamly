# Local docker-compose overrides (ADMIN_API_TOKEN, etc.) — machine-local
.env

# OS
.DS_Store

# Expo CLI (device/session cache)
.expo/

# Editors
.idea/
.vscode/

# Go (for later)
bin/
*.test
coverage.out

# Local DB backups and one-off data exports/imports — machine-local scratch
backend/activities-service/backups/
*.dump
/belgrade.json

# Superpowers process artifacts (plans, specs, scratch) — not shipped
docs/superpowers/
.superpowers/

# Agent pipeline artifacts — local paper-trail, not shipped
# (process definition: pipeline/README.md, tracked)
pipeline/*
!pipeline/README.md

# Session scratch
.claude/scheduled_tasks.lock

# Per-session isolated worktrees — ephemeral checkouts, not shipped
# (process definition: .claude/worktrees/README.md, tracked)
.claude/worktrees/*
!.claude/worktrees/README.md
